## keynote

> "description": "Keynote-New-v2 Vim + Mouse(by haku): 0. <A-Space>,<A-j>,space,<S-space>,J,K,u,U;-> Basic Toggle,Nav; 1. <A-enter> -> Play Slideshow; 2. T/1/n -> show/hide tab bar/Object Layouts/Presenter Notes; 3. <A-r>/q -> show/hide rule/object list; 4. b,<A-v>,<A-c>,<A-d> -> toggle inspector-document/arrange tools/color/Duplicate; 5. <A-z>,<A-x>,<A-Z> / x,button3,button4,button5 -> show/hide indicator, tool bar, slide only, navigator,light table / delete,draw line by self, undo,redo; 6.y -> place holder; 7. -/= -> smaller/bigger; 8. <C-r>,<C-e> -> choose file, equation; 9. <C-L>,D -> lock/unlock,duplicate; 10. 0, H, h -> real size, fit content, fit selection; 11. N,<A-n> -> new slide, new keynote; 12. g,G,tab,<C-tab>,<A-k> -> goto Slide,show slide number, Show previous/next inspector tab, skip slider ; 13. w,e,r -> toggle build order,inspector-animate,inspector-format; 14. c,v -> copy,paste style; 15. t/<C-t> -> table/text; 16. a/s/<C-a>/<C-s>/d/f/<C-d>/<C-f> -> left/center/fontleft/fontcenter/top/middle/horizontal/vertical; 17. 2,3,4,5,6 -> insert rectangle, rounded rectangle, oval, line, arrow line; 18. z -> focus on slides; Presenting: 1.<C-S> toggle mode; 2. a,s,d,f,g -> i,j,k,l,o; 3.space -> enter; 4. q -> screenbrush; 5. w,e,<C-w>,<C-e> -> before/next build,advanced before/next build; 6. r -> delete Final: esc, ctrl+enter / space/ <A-s> -> change the mode / enter + toggle",
> i : insert mode
> left_command : toggle thumbnail

## freeform

> "description": "Freeform-New-v1 Vim + Mouse(by haku) : Toggle Basic -> <A-Space>,<A-j>,mouse button1; ; add move scene -> option/space/s-space; Show/Hide Color,connectors -> A-c, r; A-s -> save as insert scene; t -> table ",
> i : insert mode
> q : insert note
> w,e : move scene left,right
> 1,2,3,4,5,6 : insert rectangle, rounded rectangle, oval, line, arrow line
> a,s,d,f : align left,center,right,top
> c,v : copy,paste style
> pointing button 1,2 : undo,redo
> A-c : show/hide color panel
> r : show/hide connectors

## preview

> A-Space, A-j : toggle keymaps
> m,n : mark, open marklist
> Preview Option/Control Remap(by haku)::Option Key: Vanilla Key: 2. j,k,g,G,u,d → move up,down,top,bottom,up page,down page; 3. o,i → back,forward item; 4. z,Z → hide/open sidebar; 5. x → hide tool bar; 6. d → delete; 7. a,s,q,w,e,r,t → annotation; 8. h → open highlight pane; 9. f → search; 10. C-enter -> click; 11. u/C-r -> undo/redo; 12. <C-d> -> Duplicate",

## wezterm

> Command-tab : option-tab
> C-h : backspace

## finder
> "description": "Finder-New-VimLike Option/Control/Command Remap (by haku):Option j/space → toggle finder_vim, Option 1. 1,2,3,4,5,6 for sorted; 2.g,i,o → google, icloud, onedrive; Vim: 1.j,k,h,l,g,G → Nav;3. o,i,u,U → child,parent,undo,redo; 3. C-o,C-i → Back,Forward; 3. p,z,t → show/hide preview,side bar,new tab; 4. r,x,n/N → rename,delete,create folder/window; 5. d,D,c,C → go to download,desktop,home,Document; 6. c,v,V → copy,paste,move paste; 7. enter → open.8.C-g → go to folder; 9. 1,2,3,4,5,6,7,tab,q,w,e,a,s,d,y → color tags; Command: tab → cycle through windows; 10. enter,esc,tab -> change from insert to normal"

## Sys-Option

> "description": "System Option Remap(haku). Option : 1. f,b,h to ←,→,delete; 2. a → select all; 3. d → look up, w → close window, q → hide window; 4. tab → command tab; 5. m , . → window fill, left right,; 6. C - A - j,k,m => Left/Right Workspace/message; 7. Control+Option+q → lock; 8. Space → Spotlight; 9. C-m,A-C-C,A-C-I,A-C-O,A-C-S,A-C-U -> mail,calendar,setting,activity,Google,Karabiner",

## Sys-Cmd
> "description": "System command Remap(haku).  left_option alone → Eisu/Kana, right_command → Chinese/En",

## Sys-Ctrl
> "description": "System Controls (by haku). 1. c, v, x, z, h → Copy, Paste, Cut, Undo, delete; 2. n,p,b,f,a,e → Down, Up, Left, Right, Home, End;  4. left_control → (left_control,esc); 4.2 left_control double pressed -> esc 5. w → switch to 73; 6. 1,2,3,4,5 → switch desktop; 7. q,Q → show wezter,qutebrowser;",


## Mail
> "description": "Mail-New-Vim (by haku): Basic Nav: j,k,u,d,g/G,l → down,up,bottom,top,filter; Flag: 1,2,3,4,5,6 ; <C-l> insert bulleted list 1. z,x,t → show/hide sidebar, toolbar, tab; 2. / → search, spacebar, p,n,s → quick look attach, scroll content down and up, save attachment  ; 3. -/= → zomm out/in; ; Option: 1. 1,2,3,4,5,0 → KU,google,126,flag,all,sent;2. n → message; 3. r/R/f → reply/reply all/forward; Mode Change: <A-enter> → send; <A-j> -> toggle mode; <A-space> -> to insert; Control: 1. '/shift ' → quote increase/decrease; 2. u,i,o → underline/italic/bold; ",
